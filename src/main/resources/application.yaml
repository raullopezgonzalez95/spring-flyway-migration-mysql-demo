spring:
  datasource:
    url: jdbc:mysql://localhost:3307/flyway_db
    username: root
    password: my-secret-pw
    driverClassName: com.mysql.cj.jdbc.Driver
  flyway:
    enabled: true
    locations: classpath:db/migration
